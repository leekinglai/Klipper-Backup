[gcode_macro MYTEST]
gcode = 
	DYNAMIC_MACRO MACRO=MYTEST {rawparams}

[gcode_macro HEAT_HOTEND]
gcode = {% set temp = params.TEMP|int %}
	DYNAMIC_MACRO MACRO=HEAT_HOTEND {rawparams}

[gcode_macro UPDATE_MAINSAIL]
gcode = 
	DYNAMIC_MACRO MACRO=UPDATE_MAINSAIL {rawparams}

[gcode_macro IDM_PROBE]
gcode = 
	DYNAMIC_MACRO MACRO=IDM_PROBE {rawparams}

